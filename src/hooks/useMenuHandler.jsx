
export default function useContentBlock(navbarlabel) {

  // if (active === 'Posts') {
  //   const getPosts = async () => {
  //     const result = await Request.GetPosts();

  //     return setContent(() => result.data.slice(0, 7))
  //   };

  //   getPosts();
  // }
}